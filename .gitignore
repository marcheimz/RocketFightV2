# ---------------------------------------------------------------------------
# Build output
#
# Everything under build/ is reproducible from CMakeLists.txt, including the
# FetchContent copies of SFML and doctest in build/_deps -- which are the reason
# this directory is measured in hundreds of megabytes and must never be tracked.
# ---------------------------------------------------------------------------
build/
build-*/
cmake-build-*/
out/
install/

# CMake droppings if anyone ever configures in-tree
CMakeCache.txt
CMakeFiles/
CMakeUserPresets.json
cmake_install.cmake
compile_commands.json

# ---------------------------------------------------------------------------
# Compiler and tooling artefacts
# ---------------------------------------------------------------------------
*.o
*.obj
*.a
*.so
*.so.*
*.dylib
*.dll
*.exe
*.gch
*.pch
*.d

# Sanitiser / profiler / coverage output
*.gcda
*.gcno
*.gcov
*.profraw
*.profdata
callgrind.out.*
perf.data
perf.data.old
# Anchored to the repository root on purpose. An unanchored "core" matches at
# any depth, which silently excludes src/core/ -- the entire physics core.
/core
core.[0-9]*
vgcore.*

# ---------------------------------------------------------------------------
# Editors and IDEs
# ---------------------------------------------------------------------------
.vscode/
.idea/
.cache/
.clangd/
*.swp
*.swo
*~
.ccls-cache/

# ---------------------------------------------------------------------------
# OS noise
# ---------------------------------------------------------------------------
.DS_Store
Thumbs.db

# ---------------------------------------------------------------------------
# Project scratch
#
# Recordings, captured frames and episode dumps are outputs of experiments, not
# inputs to them. Keep them local.
# ---------------------------------------------------------------------------
scratch/
*.log
*.png
*.replay

# The submission server's store: submitted artifacts, compiled objects, per-run
# results and the operator's weights.json. It is state a deployment owns, not
# source, and it holds other people's uploaded code by design. `--data=PATH`
# moves it somewhere outside the tree, which is what a real deployment should do.
server-data/
